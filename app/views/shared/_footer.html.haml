- if user_signed_in?
   / = link_to("Upload Dive Data", new_user_upload_path, :style => 'display:none;')
   %span#logged-in-as
      Logged in as
      = current_user.name
      |
      = link_to('Logout', destroy_user_session_path, :id => 'logout')
- else
   = link_to('Login', new_user_session_path)

|
= link_to "Contact Us", "mailto:admin@logdives.com?subject=Website%20Feedback"



|
&copy; 2011
= link_to("Logdives.com", 'http://logdives.com', :target => '_blank')


